# Oracle BI Publisher jars and the IBM Db2 JDBC driver are not redistributable
# from this repo, and DB credentials stay on the Skynet host. See README.md for
# how they get there. (template/ is the pre-registry template folder, unused now.)
vendor/
template/
.env

# Word lock files created while a template is open
~$*
