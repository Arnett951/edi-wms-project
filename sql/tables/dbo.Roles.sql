-- =====================================================
-- TABLE: dbo.Roles
-- =====================================================
CREATE TABLE dbo.Roles (
    [RoleId] int IDENTITY(1,1) NOT NULL,
    [RoleName] nvarchar(50) NOT NULL
);
